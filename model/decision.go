package model

/*
import (
	"time"

	"gorm.io/gorm"
)

// ExampleTable representa a entidade da tabela example_table no banco de dados
type Decision struct {
	gorm.Model
	ID        int64     `json:"id"`         // Chave primária db:"id"
	Document  string    `json:"document"`   // Coluna document db:"document"
	Policy    string    `json:"policy"`     // Coluna policy db:"policy"
	CreatedAt time.Time `json:"created_at"` // Coluna created_at db:"created_at"
	Payload   JSONB     `json:"payload"`    // Coluna payload db:"payload"
}

// JSONB é uma implementação personalizada para lidar com o tipo JSONB do PostgreSQL
type JSONB map[string]interface{}
*/
